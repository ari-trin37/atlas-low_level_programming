#include "main.h"
/**
* _puts_recursion - printing a string followed with new line.
* @s: our variable  
*
*/

void _puts_recursion(char *s)
{
    s = 0;
    if (s == '\0')
    {
        _putchar('\n');
        return;
    }

        _putchar('\n');
        return;
}