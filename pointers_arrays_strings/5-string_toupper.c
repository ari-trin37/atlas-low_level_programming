#include "main.h"
/**
 * string_toupper - function name
 * @char * - para one
 *
 * Return: nothing
 */

char *string_toupper(char *str)
{ 
	int i = 0

    if (i = 0; str >= 'a' && str <= 'z') 
	{
        return (str) - ('a' - 'A');
    }
    return (str);
}