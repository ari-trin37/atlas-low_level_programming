#include "main.h"
/**
 * string_toupper - function name
 * @char * - para one
 *
 * Return: nothing
 */

char *string_toupper(char *)
{ 
	char a = 0
	char z = 0

    if (char >= 'a' && chr <= 'z') 
	{
        return (char) - ('a' - 'A');
    }
    return (char);
}