#include "main.h"
/**
 * _abs - function name
 * @x : created new variable 
 *
 * Return : computes equals calculation / finding absolute value of integer.
 */

int _abs(int x)
{
	if (x < 0)
		x = x * -1; 
	return (x);
}	     
